{
  "name": "uniform-monorepo",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "pnpm --filter @uniform-dev/playground dev",
    "build": "pnpm --filter @uniform-dev/core build",
    "build:watch": "pnpm --filter @uniform-dev/core dev",
    "test": "pnpm --filter @uniform-dev/core test",
    "lint": "pnpm -r lint",
    "format": "pnpx prettier --write '{apps,packages}/**/*.{ts,tsx,js,jsx,css,scss,html}'",
    "docs:dev": "pnpm --filter docs run start",
    "docs:build": "pnpm --filter docs run build",
    "docs:serve": "pnpm --filter docs run serve"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "semver": "^7.7.4"
  }
}
